# Render Production Environment Variables
# Copy these to your Render service environment variables

NODE_ENV=production
PORT=10000

# MongoDB Atlas Connection (replace <db_username> and <db_password> with your actual credentials)
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@cluster0.rxih090.mongodb.net/smart_health_platform?retryWrites=true&w=majority

# Security Keys (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-render-2024
JWT_EXPIRE=7d
ENCRYPTION_KEY=your-32-character-encryption-key-here

# AI Service
GEMINI_API_KEY=your_gemini_api_key_here

# CORS and Frontend
FRONTEND_URL=https://your-frontend-app.onrender.com

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=info